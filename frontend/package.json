{
  "name": "formcoach-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FormCoach frontend. Works two ways with zero code changes: open index.html through any static server (js/*.js imports resolve via the CDN importmap in index.html), or `npm install && npm run dev` for a local Vite dev server (Vite resolves the same bare specifiers from node_modules instead and ignores the importmap).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.180.0",
    "kalidokit": "1.1.5",
    "@pixiv/three-vrm": "3.5.5",
    "@mediapipe/tasks-vision": "0.10.14"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
